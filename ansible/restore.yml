---
- hosts: "{{myhost}}"
  tasks:
    - name: "Restore of database {{dbname}}"
      mydump: 
        action: restore
        db: "{{dbname}}" 
        user: "root" 
        password: "asdf10" 
        path: "./{{dbname}}"
#        exclude: true 
#        tables:
#          - AssetEntry
      register: res

    - debug: var=res
